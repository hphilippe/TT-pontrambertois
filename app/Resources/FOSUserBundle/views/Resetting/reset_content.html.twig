{% trans_default_domain 'FOSUserBundle' %}

<br>

{{ form_start(form, { 'action': path('fos_user_resetting_reset', {'token': token}), 'attr': { 'class': 'fos_user_resetting_reset col-md-12 go-right special_form' } }) }}
<div class="form-group">
    {{ form_widget(form.plainPassword.first, {'attr': {'class': 'form-control'}}) }}
    {{ form_label(form.plainPassword.first) }}
    {{ form_errors(form.plainPassword.first) }}
</div>
<div class="form-group">
    {{ form_widget(form.plainPassword.second, {'attr': {'class': 'form-control'}}) }}
    {{ form_label(form.plainPassword.second) }}
    {{ form_errors(form.plainPassword.second) }}
</div>

<div class="form-group">
    <div class="col-md-offset-3 col-xs-6 ">
        <button class="btn btn-primary btn-block btn-lg">{{ 'resetting.reset.submit'|trans }}</button>
    </div>
</div>
{{ form_end(form) }}

