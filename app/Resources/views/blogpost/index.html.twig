{% extends 'base.html.twig' %}

{% block body %}
    

    {#<table>
        <thead>
            <tr>
                <th>Title</th>
                <th>Content</th>
                <th>Createdat</th>
                <th>Id</th>
                <th>author</th>
                <th>categorie</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for blogPost in blogPosts %}
            <tr>
                <td><a href="{{ path('blogpost_show', { 'id': blogPost.id }) }}">{{ blogPost.title }}</a></td>
                <td>{{ blogPost.content }}</td>
                <td>{% if blogPost.createdAt %}{{ blogPost.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ blogPost.id }}</td>
                <td>{{ blogPost.author }}</td>
                <td>{{ blogPost.categorie }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('blogpost_show', { 'id': blogPost.id }) }}">show</a>
                        </li>
                        {% if is_granted('ROLE_ADMIN') %}   
                        <li>
                            <a href="{{ path('blogpost_edit', { 'id': blogPost.id }) }}">edit</a>
                        </li>
                        {% endif %}
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>#}
    <br><br>
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    
                <a style="margin-bottom: 15px;margin-left: 10px;"  class="btn btn-primary" href="{{ path('administration') }}">Retour à l'administration général</a>
                <a style="margin-bottom: 15px;margin-left: 10px;"  class="btn btn-primary" href="{{ path('blogcomment_index') }}">Accès à l'administration des commentaires</a>

                    <div class="panel panel-default panel-table">
                        <div class="panel-heading">
                            <div class="row">
                                <div class="col col-xs-6">
                                    <h3 style="font-weight: bold; font-size: 20px" class="panel-title">Liste de tous les articles</h3>
                                </div>
                                <div class="col col-xs-6 text-right">
                                    <!--<button type="button" class="btn btn-sm btn-primary btn-create">Create New</button>-->
                                    {% if is_granted('ROLE_ADMIN') %}        
                                        <a class="btn btn-sm btn-primary btn-create" href="{{ path('blogpost_new') }}">créer un nouvel article</a>
                                    {% endif %}
                                </div>
                            </div>
                        </div>
                        <div class="panel-body">
                            <table class="table table-striped table-bordered table-list">
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th class="hidden-xs">Titre</th>
                                        <th>Date de création</th>
                                        <th>Auteur</th>
                                        <th>Catégorie</th>
                                        <th><em class="fa fa-cog"></em></th>
                                    </tr> 
                                </thead>
                                <tbody>
                                    {% for blogPost in blogPosts %}
                                        <tr>
                                            <td>{{ blogPost.id }}</td>
                                            <td><a href="{{ path('blogpost_show', { 'id': blogPost.id }) }}">{{ blogPost.title }}</a></td>
                                            <td>{% if blogPost.createdAt %}{{ blogPost.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                                            <td>{{ blogPost.author }}</td>
                                            <td>{{ blogPost.categorie }}</td>
                                            <td align="center">
                                                <a class="btn btn-default" href="{{ path('blogpost_show', { 'id': blogPost.id }) }}"><em class="fa fa-eye"></em></a>
                                                {% if is_granted('ROLE_ADMIN') %}   
                                                    <a class="btn btn-primary" href="{{ path('blogpost_edit', { 'id': blogPost.id }) }}"><em class="fa fa-pencil"></em></a>
                                                {% endif %}
                                            </td>
                                        </tr>
                                    {% endfor %}
                                    <!--<tr>
                                        <td class="hidden-xs">1</td>
                                        <td>John Doe</td>
                                        <td>John Doe</td>
                                        <td>johndoe@example.com</td>
                                        <td align="center">
                                            <a class="btn btn-default"><em class="fa fa-pencil"></em></a>
                                            <a class="btn btn-danger"><em class="fa fa-trash"></em></a>
                                        </td>
                                    </tr>-->
                                </tbody>
                            </table>
                        </div>
                        <div class="panel-footer">
                            <div class="row">
                               <!-- <div class="col col-xs-4">Page 1 of 5
                                </div>
                                <div class="col col-xs-8">
                                    <ul class="pagination hidden-xs pull-right">
                                        <li><a href="#">1</a></li>
                                        <li><a href="#">2</a></li>
                                        <li><a href="#">3</a></li>
                                        <li><a href="#">4</a></li>
                                        <li><a href="#">5</a></li>
                                    </ul>
                                    <ul class="pagination visible-xs pull-right">
                                        <li><a href="#">«</a></li>
                                        <li><a href="#">»</a></li>
                                    </ul>
                                </div>-->
                            </div>
                        </div>
                    </div>
               
                
                </div>
            </div>
        </div>

{% endblock %}
