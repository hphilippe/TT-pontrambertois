{% extends 'base.html.twig' %}

{% block body %}
    <h1>Multimedia list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Categorie</th>
                <th>Path</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for multimedia in multimedia %}
            <tr>
                <td><a href="{{ path('adminMultimedia_show', { 'id': multimedia.id }) }}">{{ multimedia.id }}</a></td>
                <td>{{ multimedia.categorie }}</td>
                <td>{{ multimedia.path }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('adminMultimedia_show', { 'id': multimedia.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('adminMultimedia_edit', { 'id': multimedia.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('adminMultimedia_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
